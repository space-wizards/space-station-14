name: "Labels: Sprites"

on:
  pull_request:
    paths:
      - '**.rsi**'

jobs:
  add_label:
    runs-on: ubuntu-latest
    steps:
    - uses: actions-ecosystem/action-add-labels@v1
      with:
        labels: "Changes: Sprites"
