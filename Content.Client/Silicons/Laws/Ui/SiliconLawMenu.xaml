<controls:FancyWindow xmlns="https://spacestation14.io"
                      xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls"
                      xmlns:gfx="clr-namespace:Robust.Client.Graphics;assembly=Robust.Client"
                      Title="{Loc 'laws-ui-menu-title'}"
                      MinSize="200 100"
                      SetSize="450 515">
    <BoxContainer Orientation="Vertical"
                  HorizontalExpand="True"
                  VerticalExpand="True">
        <PanelContainer VerticalExpand="True" Margin="10 10 10 10">
            <PanelContainer.PanelOverride>
                <gfx:StyleBoxFlat BackgroundColor="#1B1B1E" />
            </PanelContainer.PanelOverride>
            <ScrollContainer
                HScrollEnabled="False"
                HorizontalExpand="True"
                VerticalExpand="True">
                <BoxContainer
                    Name="LawDisplayContainer"
                    Orientation="Vertical"
                    VerticalExpand="True"
                    Margin="10 10 10 0">
                </BoxContainer>
            </ScrollContainer>
        </PanelContainer>
        <OptionButton
            Name="LawChatChannelOption"
            Margin="10 0 10 10" />
        <Button
            Name="LawAnnounceButton"
            Margin="10 0 10 10" />
        <!-- Footer -->
        <controls:StripeBack>
        <BoxContainer Orientation="Vertical">
            <PanelContainer/>
            <!--Potential Todo: Change Logo and Version # based on Laws-->
            <BoxContainer Name="Footer" Orientation="Horizontal" Margin="10 2 5 0" VerticalAlignment="Bottom">
                <Label Text="{Loc 'laws-window-footer-left'}" StyleClasses="WindowFooterText" />
                <Label Name="Version" Text="{Loc 'laws-window-footer-right'}" StyleClasses="WindowFooterText"
                        HorizontalAlignment="Right" HorizontalExpand="True"  Margin="0 0 5 0" />
                <TextureRect StyleClasses="NTLogoDark" Stretch="KeepAspectCentered"
                        VerticalAlignment="Center" HorizontalAlignment="Right" SetSize="19 19"/>
            </BoxContainer>
        </BoxContainer>
        </controls:StripeBack>
    </BoxContainer>
</controls:FancyWindow>

