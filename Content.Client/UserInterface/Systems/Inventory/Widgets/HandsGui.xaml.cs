using Content.Client.UserInterface.Systems.Inventory.Controls;
using Content.Shared.Hands.Components;
using Robust.Client.AutoGenerated;
using Robust.Client.UserInterface.Controls;
using Robust.Client.UserInterface.XAML;

namespace Content.Client.UserInterface.Systems.Inventory.Widgets;

[GenerateTypedNameReferences]
public sealed partial class HandsGui : UIWidget
{
    public ItemStatusPanel StatusPanel { get; }

    public HandsGui()
    {
        RobustXamlLoader.Load(this);

        // TODO hud refactor define this in xaml
        // TODO hud refactor BEFORE MERGE fix this pushing down hand controls when picking up an item
        StatusPanel = ItemStatusPanel.FromSide(HandLocation.Middle);
        StatusContainer.AddChild(StatusPanel);
        StatusPanel.SetPositionFirst();
        StatusPanel.Update(null);

        var inventoryUiController = UserInterfaceManager.GetUIController<InventoryUIController>();
        inventoryUiController.RegisterHandContainer(HandContainer);
    }
}
