using System.Numerics;
using Content.Client.Stylesheets;
using Content.Client.UserInterface.Systems.Chat.Widgets;
using Content.Shared.CCVar;
using Robust.Client.AutoGenerated;
using Robust.Client.UserInterface.Controls;
using Robust.Client.UserInterface.XAML;
using Robust.Shared.Configuration;

namespace Content.Client.UserInterface.Screens;

[GenerateTypedNameReferences]
public sealed partial class SeparatedChatGameScreen : InGameScreen
{
    public const string StyleClassChatContainer = "ChatContainer";
    public const string StyleClassChatOutput = "ChatOutput";

    [Dependency] private IConfigurationManager _cfg = default!;

    public SeparatedChatGameScreen()
    {
        IoCManager.InjectDependencies(this);
        RobustXamlLoader.Load(this);

        AutoscaleMaxResolution = new Vector2i(1080, 770);

        SetAnchorPreset(ScreenContainer, LayoutPreset.Wide);
        SetAnchorPreset(ViewportContainer, LayoutPreset.Wide);
        SetAnchorPreset(MainViewport, LayoutPreset.Wide);
        SetAnchorAndMarginPreset(Inventory, LayoutPreset.BottomLeft, margin: 5);
        SetAnchorAndMarginPreset(TopLeftContainer, LayoutPreset.TopLeft, margin: 10);
        SetAnchorAndMarginPreset(Ghost, LayoutPreset.BottomWide, margin: 80);
        SetAnchorAndMarginPreset(Hotbar, LayoutPreset.BottomWide, margin: 5);
        SetAnchorAndMarginPreset(Alerts, LayoutPreset.CenterRight, margin: 10);

        ScreenContainer.OnSplitCenterChanging += ScreenContainerOnSplitCenterChanging;
        ScreenContainer.OnSplitResizeFinished += () =>
        {
            OnChatResized?.Invoke(new Vector2(ScreenContainer.SplitFraction, 0));
        };

        ViewportContainer.OnResized += ResizeActionContainer;
    }

    private void ResizeActionContainer()
    {
        float indent = 20;
        Actions.ActionsContainer.MaxGridWidth = ViewportContainer.Size.X - indent;
    }

    public override ChatBox ChatBox => GetWidget<ChatBox>()!;

    public override void SetChatSize(Vector2 size)
    {
        ScreenContainer.ResizeMode = SplitContainer.SplitResizeMode.RespectChildrenMinSize;
    }

    private void ScreenContainerOnSplitCenterChanging(SplitContainer.SplitCenterChangingEventArgs args)
    {
        if (!args.Dragging)
            return;

        var snapDistance = _cfg.GetCVar(CCVars.ViewportSeparatedSnapTolerance);
        if (snapDistance <= 0)
            return;

        var viewportWidth = (int) MathF.Round((args.SplitCenter - ScreenContainer.SplitWidth / 2) * UIScale);
        var viewportHeight = ViewportContainer.PixelHeight;

        if (viewportWidth <= 0 || viewportHeight <= 0)
            return;

        var drawWidth = MainViewport.Viewport.GetDrawBox(new Vector2i(viewportWidth, viewportHeight)).Width;
        var fillerWidth = viewportWidth - drawWidth;

        if (MathF.Abs(fillerWidth) > snapDistance * UIScale)
            return;

        args.SplitCenter = drawWidth / UIScale + ScreenContainer.SplitWidth / 2;
    }
}
