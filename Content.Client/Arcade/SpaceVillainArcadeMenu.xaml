<DefaultWindow
    xmlns="https://spacestation14.io"
    Title="{Loc 'space-villain-menu-title'}"
    MinWidth="540"
    MinHeight="225">
    <GridContainer
        Columns="1"
        HorizontalAlignment="Stretch"
        VerticalAlignment="Stretch"
        HorizontalExpand="True"
        VerticalExpand="True">
        <!-- Info grid -->
        <GridContainer
            Columns="3"
            HorizontalAlignment="Stretch"
            HorizontalExpand="True">
            <!-- Player name -->
            <Label
                Text="{Loc 'space-villain-menu-label-player'}"
                Align="Center"
                HorizontalExpand="True"/>
            <!-- Separator -->
            <Label
                Text="|"
                Align="Center"/>
            <!-- Enemy name -->
            <Label
                Text="{Loc 'space-villain-menu-label-enemy'}"
                Name="EnemyNameLabel"
                Align="Center"
                HorizontalExpand="True"/>
            <!-- Player stats -->
            <GridContainer
                Columns="2"
                HorizontalAlignment="Stretch"
                HorizontalExpand="True">
                <!-- Player HP -->
                <Label
                    Name="PlayerHpLabel"
                    Align="Center"
                    HorizontalExpand="True"
                    Modulate="#37e928"/>
                <!-- Player MP -->
                <Label
                    Name="PlayerMpLabel"
                    Align="Center"
                    HorizontalExpand="True"
                    Modulate="#3483ff"/>
            </GridContainer>
            <!-- Separator -->
            <Label
                Text="|"
                Align="Center"/>
            <!-- Enemy stats -->
            <GridContainer
                Columns="2"
                HorizontalAlignment="Stretch"
                HorizontalExpand="True">
                <!-- Enemy HP -->
                <Label
                    Name="EnemyHpLabel"
                    Align="Center"
                    HorizontalExpand="True"
                    Modulate="#e94740"/>
                <!-- Enemy MP -->
                <Label
                    Name="EnemyMpLabel"
                    Align="Center"
                    HorizontalExpand="True"
                    Modulate="#3483ff"/>
            </GridContainer>
        </GridContainer>
        <!-- Spacer -->
        <BoxContainer VerticalExpand="True"/>
        <!-- Player and enemy action messages -->
        <Label Name="PlayerActionLabel"
                Align="Left"/>
        <Label Name="EnemyActionLabel"
                Align="Right"/>
        <!-- Spacer -->
        <BoxContainer VerticalExpand="True"/>
        <!-- Action button grid -->
        <BoxContainer
            Orientation="Horizontal"
            HorizontalAlignment="Stretch"
            HorizontalExpand="True">
            <Button
                Name="AttackButton"
                Text="{Loc space-villain-menu-button-attack}"
                StyleClasses="OpenRight"
                HorizontalExpand="True"/>
            <Button
                Name="HealButton"
                Text="{Loc space-villain-menu-button-heal}"
                StyleClasses="ButtonSquare"
                HorizontalExpand="True"/>
            <Button
                Name="RechargeButton"
                Text="{Loc space-villain-menu-button-recharge}"
                StyleClasses="OpenLeft"
                HorizontalExpand="True"/>
        </BoxContainer>
        <!-- New game button -->
        <Button
            Name="NewGameButton"
            Text="{Loc space-villain-menu-button-new-game}"
            HorizontalExpand="True"/>
    </GridContainer>
</DefaultWindow>
