<DefaultWindow xmlns="https://spacestation14.io"
               xmlns:graphics="clr-namespace:Robust.Client.Graphics;assembly=Robust.Client"
               xmlns:xNamespace="http://schemas.microsoft.com/winfx/2006/xaml"
               xmlns:style="clr-namespace:Content.Client.Stylesheets"
               Title="{Loc 'late-join-gui-title'}"
               SetSize="685 560" MinSize="685 560">
    <BoxContainer Orientation="Horizontal" HorizontalExpand="True" VerticalExpand="True">
            <ScrollContainer VerticalExpand="True" HorizontalExpand="True" MinSize="325 0">
                <BoxContainer Name="CharacterList" Orientation="Vertical"/>
            </ScrollContainer>

        <PanelContainer MinSize="2 0">
            <PanelContainer.PanelOverride>
                <graphics:StyleBoxFlat BackgroundColor="{xNamespace:Static style:StyleNano.NanoGold}" ContentMarginTopOverride="2" />
            </PanelContainer.PanelOverride>
        </PanelContainer>

        <BoxContainer Name="JobList" Orientation="Vertical" VerticalExpand="True" HorizontalExpand="True" MinSize="360 0" />

    </BoxContainer>
</DefaultWindow>
