using Content.Client.Message;
using Content.Client.Options.UI;
using Content.Client.UserInterface.Systems.EscapeMenu;
using Robust.Client.AutoGenerated;
using Robust.Client.Console;
using Robust.Client.UserInterface;
using Robust.Client.UserInterface.XAML;

namespace Content.Client.Lobby.UI
{
    [GenerateTypedNameReferences]
    public sealed partial class LobbyGui : UIScreen
    {
        [Dependency] private readonly IClientConsoleHost _consoleHost = default!;

        private ObserveWarningWindow? _observeWarning;
        private OptionsMenu? _optionsMenu;

        public LobbyGui()
        {
            RobustXamlLoader.Load(this);
            IoCManager.InjectDependencies(this);
            SetAnchorPreset(MainContainer, LayoutPreset.Wide);
            SetAnchorPreset(Background, LayoutPreset.Wide);

            LobbySong.SetMarkup(Loc.GetString("lobby-state-song-no-song-text"));

            LeaveButton.OnPressed += _ => _consoleHost.ExecuteCommand("disconnect");

            var optionsController = UserInterfaceManager.GetUIController<OptionsUIController>();

            OptionsButton.OnPressed += _ =>
            {
                _optionsMenu = optionsController.ToggleWindow();

                if (_optionsMenu is { IsOpen: true })
                {
                    _optionsMenu.OnClose += () => OptionsButton.Pressed = false;
                }
            };

            ObserveButton.OnPressed += _ =>
            {
                if (_observeWarning is { IsOpen: true })
                {
                    _observeWarning.Close();

                    return;
                }

                _observeWarning = new ObserveWarningWindow();

                _observeWarning.OnClose += () => ObserveButton.Pressed = false;

                _observeWarning.OpenCentered();
            };
        }

        protected override void ExitedTree()
        {
            base.ExitedTree();

            if (_optionsMenu is { IsOpen: true })
            {
                _optionsMenu.Close();
            }

            if (_observeWarning is { IsOpen: true })
            {
                _observeWarning.Close();
            }
        }

        public void SwitchState(LobbyGuiState state)
        {
            DefaultState.Visible = false;
            CharacterSetupState.Visible = false;

            switch (state)
            {
                case LobbyGuiState.Default:
                    DefaultState.Visible = true;
                    RightSide.Visible = true;
                    break;
                case LobbyGuiState.CharacterSetup:
                    CharacterSetupState.Visible = true;

                    var actualWidth = (float) UserInterfaceManager.RootControl.PixelWidth;
                    var setupWidth = (float) LeftSide.PixelWidth;

                    if (1 - (setupWidth / actualWidth) > 0.30)
                    {
                        RightSide.Visible = false;
                    }

                    UserInterfaceManager.GetUIController<LobbyUIController>().ReloadCharacterSetup();

                    break;
            }
        }

        public enum LobbyGuiState : byte
        {
            /// <summary>
            ///  The default state, i.e., what's seen on launch.
            /// </summary>
            Default,
            /// <summary>
            ///  The character setup state.
            /// </summary>
            CharacterSetup
        }
    }
}
