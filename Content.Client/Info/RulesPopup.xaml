<Control xmlns="https://spacestation14.io"
              xmlns:parallax="clr-namespace:Content.Client.Parallax"
              xmlns:info="clr-namespace:Content.Client.Info"
              VerticalExpand="True" HorizontalExpand="True" MouseFilter="Stop">
    <parallax:ParallaxControl />
    <Control VerticalExpand="True" MaxWidth="650">
        <PanelContainer StyleClasses="windowPanel" />
        <ScrollContainer HScrollEnabled="False">
            <BoxContainer Orientation="Vertical">
                <info:RulesControl/>
                <BoxContainer Orientation="Horizontal">
                    <Button Name="ButtonAccept" Text="{Loc 'ui-rules-accept'}"/>
                    <Button Name="ButtonQuit" Text="{Loc 'ui-escape-quit'}"/>
                </BoxContainer>
            </BoxContainer>
        </ScrollContainer>
    </Control>
</Control>
