<ui:FancyWindow xmlns="https://spacestation14.io"
    xmlns:ui="clr-namespace:Content.Client.UserInterface.Controls"
    MinSize="600 400"
    Title="{Loc 'comms-console-menu-title'}">
    <BoxContainer Name="Contents">
        <BoxContainer Name="hbox"
            Orientation="Horizontal"
            HorizontalExpand="True"
            VerticalExpand="True">
            <Control MinSize="100 0" />

            <BoxContainer Name="vbox"
                Orientation="Vertical"
                HorizontalExpand="True"
                VerticalExpand="True" >
                <TextEdit Name="MessageInput"
                    HorizontalExpand="True"
                    VerticalExpand="True"
                    SizeFlagsStretchRatio="1"
                    Margin="0 0 0 5"
                    MinHeight="100"
                    PlaceHolder="{Loc 'comms-console-menu-announcement-placeholder'}" />
                <Control MinSize="0 10" HorizontalExpand="True" />
                <Button Name="AnnounceButton" Access="Public"
                    Text="{Loc 'comms-console-menu-announcement-button'}" />

                <PanelContainer StyleClasses="PanelBackgroundBaseDark" Margin="12">
                    <GridContainer Columns="2">
                        <Label Text="{Loc 'comms-console-current-alert-label'}"  />
                        <Label Name="AlertLevelLight" MinSize="100 24" />
                        <OptionButton Name="AlertLevelOptions" Access="Public"/>
                        <Button Name="SetAlertLevelButton" Access="Public" Text="{Loc 'comms-console-menu-set-alert-button-label'}" Margin="6"/>
                    </GridContainer>
                </PanelContainer>

                <BoxContainer Orientation="Horizontal" HorizontalExpand="True">

                    <!-- Call emergency shuttle button -->
                    <BoxContainer HorizontalExpand="False" HorizontalAlignment="Center" Margin="0 0 6 0">
                        <ui:StripeBack HasTopEdge="False" HasBottomEdge="False">
                            <BoxContainer Orientation="Vertical"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Bottom"
                                Margin="12 24 12 12">
                                <Label Text="{Loc 'comms-console-menu-call-shuttle'}" />
                                <Button Name="EmergencyShuttleCallButton" Access="Public"
                                    Margin="12" SetSize="80 80"
                                    StyleClasses="ButtonColorRed" />
                            </BoxContainer>
                        </ui:StripeBack>
                    </BoxContainer>

                    <BoxContainer HorizontalExpand="False" HorizontalAlignment="Center" Margin="6 0 0 0">
                        <ui:StripeBack HasTopEdge="False" HasBottomEdge="False">
                            <BoxContainer Orientation="Vertical">
                                <BoxContainer Margin="12 6 12 6">
                                    <Button Name="ShuttleIncomingLight" StyleClasses="ButtonColorGreen" Margin="0 0 6 0"/> <!-- todo.eoin Need a proper control here -->
                                    <Label Text="{Loc 'comms-shuttle-incoming-label'}" Margin="0 0 6 0" />
                                </BoxContainer>

                                <!-- Shuttle "arriving in" label here -->
                                <BoxContainer HorizontalExpand="True" Orientation="Horizontal" Margin="12 6 12 6">
                                    <Label Text="{Loc 'comms-shuttle-arriving-duration-label'}" Margin="0 0 6 0" />
                                    <BoxContainer HorizontalExpand="True" HorizontalAlignment="Right">
                                        <PanelContainer StyleClasses="PanelBackgroundBaseDark">
                                            <Label Name="_countdownLabel" StyleClasses="LabelLCDBig"/>
                                        </PanelContainer>
                                    </BoxContainer>
                                </BoxContainer>

                                <!-- Button to recall shuttle -->
                                <BoxContainer Orientation="Vertical"
                                    HorizontalAlignment="Center" Margin="12 24 12 12">
                                    <Label Text="{Loc 'comms-console-menu-recall-shuttle'}" />
                                    <Button Name="EmergencyShuttleRecallButton" Access="Public"
                                        Margin="12" SetSize="80 80"
                                        StyleClasses="ButtonColorRed" />
                                </BoxContainer>
                            </BoxContainer>
                        </ui:StripeBack>
                    </BoxContainer>
                </BoxContainer>
            </BoxContainer>

            <Control MinSize="100 0" />
        </BoxContainer>
    </BoxContainer>
</ui:FancyWindow>
