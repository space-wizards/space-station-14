<Control
    xmlns="https://spacestation14.io"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:cc="clr-namespace:Content.Client.Administration.UI.CustomControls"
    xmlns:rt="clr-namespace:Content.Client.Administration.UI.Tabs.RoundTab"
    Margin="4"
    MinSize="50 50">
    <GridContainer
        Columns="3">
        <cc:CommandButton Command="startround" Text="{Loc administration-ui-round-tab-start-round}" />
        <cc:CommandButton Command="endround" Text="{Loc administration-ui-round-tab-end-round}" />
        <cc:CommandButton Command="restartround" Text="{Loc administration-ui-round-tab-restart-round}" />
        <cc:CommandButton Command="restartroundnow" Text="{Loc administration-ui-round-tab-restart-round-now}" />
        <cc:CommandButton Name="Delay" Command="delaystart" Text="{Loc administration-ui-round-tab-delay-start}" ToggleMode="True" />
        <cc:UICommandButton Command="showactivegamerules" Text="{Loc administration-ui-round-tab-active-gamerules}" WindowType="{x:Type rt:ActiveGameRulesWindow}" />
    </GridContainer>
</Control>
