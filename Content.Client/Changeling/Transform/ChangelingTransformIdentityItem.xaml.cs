using Content.Client.CharacterInfo;
using Content.Shared.Changeling.Transform;
using Robust.Client.AutoGenerated;
using Robust.Client.UserInterface;
using Robust.Client.UserInterface.XAML;

namespace Content.Client.Changeling.Transform;

[GenerateTypedNameReferences]
public sealed partial class ChangelingTransformIdentityItem : Control
{
    public ChangelingTransformIdentityItem(ChangelingIdentityData data, IEntityManager entityManager)
    {
        RobustXamlLoader.Load(this);
        //Icon.Texture = spriteSystem.Frame0(set.Sprite);
        // var a = entityManager.GetEntity(data.Identity);
        // SpriteView.SetEntity(a);
        IdentityName.Text = data.Name;
        IdentityDescription.Text = data.Description;
        IdentityButton.Text = Loc.GetString("changeling-transform-menu-button");
        //IdentityButton.ModulateSelfOverride = set.Selected ? new Color(40, 84, 35) : new Color(68, 75, 103);

    }
}
