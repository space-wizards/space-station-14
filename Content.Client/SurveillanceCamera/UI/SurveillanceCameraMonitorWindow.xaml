<DefaultWindow xmlns="https://spacestation14.io"
               xmlns:viewport="clr-namespace:Content.Client.Viewport"
               Title="Remember to localize this, dumpy">
    <BoxContainer Orientation="Horizontal">
        <BoxContainer Orientation="Vertical" MaxWidth="200" VerticalExpand="True">
            <!-- lazy -->
            <OptionButton Name="SubnetSelector" />
            <Button Name="SubnetRefreshButton" Text="Refresh subnets" />
            <ScrollContainer VerticalExpand="True">
                <ItemList Name="SubnetList" />
            </ScrollContainer>
            <Button Name="CameraRefreshButton" Text="Refresh cameras" />
        </BoxContainer>
        <Control VerticalExpand="True" HorizontalExpand="True" Margin="5 5 5 5" Name="CameraViewBox">
            <!-- TODO: This should be a textured box so there can be static.
                 Also, find an animated texture of static. -->
            <Control VerticalExpand="True" HorizontalExpand="True" MinSize="500 500" Name="CameraViewBackground" />
            <viewport:ScalingViewport Name="CameraView"
                                      VerticalExpand="True"
                                      HorizontalExpand="True"
                                      MinSize="500 500"
                                      MouseFilter="Ignore" />
        </Control>
    </BoxContainer>
</DefaultWindow>
