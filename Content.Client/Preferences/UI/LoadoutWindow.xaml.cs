using Content.Client.UserInterface.Controls;
using Content.Shared.Preferences.Loadouts;
using Content.Shared.Preferences.Loadouts.Effects;
using Robust.Client.AutoGenerated;
using Robust.Client.UserInterface.XAML;
using Robust.Shared.Prototypes;

namespace Content.Client.Preferences.UI;

[GenerateTypedNameReferences]
public sealed partial class LoadoutWindow : FancyWindow
{
    public event Action<ProtoId<LoadoutGroupPrototype>, ProtoId<LoadoutPrototype>?>? OnLoadoutPressed;

    private List<LoadoutGroupContainer> _groups = new();

    public LoadoutWindow(RoleLoadout loadout, RoleLoadoutPrototype proto, IPrototypeManager protoManager)
    {
        RobustXamlLoader.Load(this);

        foreach (var group in proto.Groups)
        {
            var container = new LoadoutGroupContainer(loadout, protoManager.Index(group));
            LoadoutGroupsContainer.AddChild(container);
            _groups.Add(container);

            container.OnLoadoutPressed += args =>
            {
                OnLoadoutPressed?.Invoke(group, args);
            };
        }
    }

    public void RefreshLoadouts(RoleLoadout loadout)
    {
        foreach (var group in _groups)
        {
            group.RefreshLoadouts(loadout);
        }
    }
}
