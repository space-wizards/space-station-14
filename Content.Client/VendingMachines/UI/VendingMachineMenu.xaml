<controls:FancyWindow xmlns="https://spacestation14.io"
    xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls"
    xmlns:style="clr-namespace:Content.Client.Stylesheets"
    MinSize="750 500">
    <BoxContainer Orientation="Horizontal" Margin="8">
        <!-- List of vendable items -->
        <ScrollContainer MinWidth="225" MinHeight="175" SizeFlagsStretchRatio="9" HorizontalExpand="True" VerticalExpand="True">
            <BoxContainer Name="VendingContents" Orientation="Vertical" HorizontalExpand="True" VerticalExpand="True" />
        </ScrollContainer>

        <Control MinSize="8 8" />

        <!-- Item Information -->
        <BoxContainer Orientation="Vertical" MinWidth="175" SizeFlagsStretchRatio="1" HorizontalExpand="True" VerticalExpand="True">
            <Button Disabled="True">
                <BoxContainer Orientation="Vertical">
                    <!-- This gets filled with information about the currently selected item -->
                    <BoxContainer Name="VendingInfo" Orientation="Vertical" HorizontalExpand="False" VerticalExpand="True" Margin="8" />

                    <Control VerticalExpand="True" VerticalAlignment="Stretch" />

                    <!-- Gets enabled if an item is selected -->
                    <Button Name="VendButton" Text="Vend" Disabled="True" VerticalAlignment="Bottom" Margin="8" />
                </BoxContainer>
            </Button>
        </BoxContainer>
    </BoxContainer>
</controls:FancyWindow>
