<controls:FancyWindow xmlns="https://spacestation14.io"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls"
            Title="{Loc npc-window-title}"
            MinSize="200 200">
    <BoxContainer Name="Options" Orientation="Vertical" Margin="8 8">
        <controls:StripeBack>
            <Label Text="{Loc npc-window-section-npc}" HorizontalAlignment="Center"/>
        </controls:StripeBack>
        <BoxContainer Name="NPCBox" Orientation="Vertical">
            <CheckBox Name="NPCThonk" Text="{Loc npc-window-thonk}"/>
        </BoxContainer>
        <controls:StripeBack>
            <Label Text="{Loc npc-window-section-pathfinder}" HorizontalAlignment="Center"/>
        </controls:StripeBack>
        <BoxContainer Name="PathfinderBox" Orientation="Vertical">
            <CheckBox Name="PathCrumbs" Text="{Loc npc-window-path-breadcrumbs}"/>
            <CheckBox Name="PathPolys" Text="{Loc npc-window-path-polygons}"/>
            <CheckBox Name="PathNeighbors" Text="{Loc npc-window-path-neighbors}"/>
            <CheckBox Name="PathRouteCosts" Text="{Loc npc-window-path-route-costs}"/>
            <CheckBox Name="PathRoutes" Text="{Loc npc-window-path-routes}"/>
        </BoxContainer>
    </BoxContainer>
</controls:FancyWindow>
