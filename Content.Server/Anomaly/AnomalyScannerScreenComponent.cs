namespace Content.Server.Anomaly;

/// <summary>
/// This is just an empty component for the server to instantiate from prototypes.
/// Server code doesn't actually do anything with this.
/// Look at the Content Client version.
/// </summary>
[RegisterComponent]
public sealed partial class AnomalyScannerScreenComponent : Component;
