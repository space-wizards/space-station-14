- type: gameMap
  id: Opening
  mapName: 'Opening'
  mapPath: /Maps/opening.yml
  minPlayers: 0
#  maxPlayers: 80
  stations:
    Opening:
      stationProto: StandardStationArena
      components:
        - type: StationNameSetup
          mapNameTemplate: '{0} Unknown Facility {1}'
          nameGenerator:
            !type:NanotrasenNameGenerator
            prefixCreator: 'SC'
#        - type: StationEmergencyShuttle
#          emergencyShuttlePath: /Maps/Shuttles/emergency.yml
        - type: StationJobs
          availableJobs:
            Passenger: [ -1, -1 ] #infinite, not counted

