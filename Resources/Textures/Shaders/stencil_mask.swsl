void fragment() {
    // Bypasses zTexture because only alpha is queried.
    highp float alpha = texture2D(TEXTURE, UV).a;
    if (alpha == 0.0) {
        discard; // Discard so there is a hole in the stencil buffer.
    }

    COLOR = vec4(0.0);
}
