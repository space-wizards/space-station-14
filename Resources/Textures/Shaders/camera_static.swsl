const highp float StaticStrength = 0.1;

void fragment() {
    highp vec2 randomInput = FRAGCOORD.xy * SCREEN_PIXEL_SIZE + vec2(fract(TIME));
    highp float noise = zRandom(randomInput).x;
    COLOR = vec4(vec3(noise * StaticStrength), 1.0);
}
