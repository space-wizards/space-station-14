command-description-bwoink-message =
    Sends a message in the given channel for the given sessions.
command-description-bwoink-addchannel =
    Adds a user to a given bwoink channel allow list. Only does something for channels that use the list.
command-description-bwoink-rmchannel =
    Removes a user from a given bwoink channel allow list. Only does seomthing for channels that use the list.
command-description-bwoink-sync =
    Synchronizes the entire bwoink state for a given session. This can be done in cases where the Conversations change server side (for example VV) and the client is not told about it.

command-description-bwoink-1984 = Cannot manage channel {$proto}, message has not been sent.
